# Integration test report

## Статические проверки
- Все Python-файлы: `compileall` — OK.
- Проверка локальных `from module import symbol` по AST — 0 отсутствующих символов.
- `tv_board.py` SHA-256 совпадает с рабочим исходником: `fdc96c495e11082af449e01cbbf6d3f3115b212ee2a841126c20a6fb63ec031e`.

## Турнир
- Проверена активная версия: `v3_2026_09_top7`.
- Множители: 1=2.0, 2=1.9, 3=1.8, 4=1.7, 5=1.6, 6=1.5, 7=1.4.
- Идеальный пилот, 1 место во всех дисциплинах: 970 очков (200 + 220 + 20 + 240 + 30 + 260).

## Миграция на копии рабочей valevo.db
До: 126 pilots / 110 laps / 100 season_awards / 41 bonus_wallet / 43 time_requests, integrity=ok.
После первого запуска: те же значения, новые referrals=0 и roulette_spins=0, integrity=ok; создан backup; participants=20.
После второго запуска: те же значения, integrity=ok; новый backup не создаётся — миграция идемпотентна.
Уведомления после миграции: 126/126 пилотов `notify_standings=1`.

## Рефералы
- Создание реферала с бонусом 350 ₽ — OK.
- Повторный реферал для того же нового пользователя — отклонён уникальным ограничением.

## Ограничение среды проверки
Полный сетевой smoke-test Telegram/YCLIENTS не выполнялся: тестовый раннер не имеет внешнего доступа. Схема БД, миграция, формула и интеграционные импорты проверены локально; реальные Telegram/YCLIENTS credentials не использовались.
